\section{Simulation Playbook (Minimal Viable Demos)}\label{sec:simulation-playbook}
Grid-based split-step evolution of a Gaussian packet on $(x,y)$ with a discrete $\theta$ ladder demonstrates cross-Hall drift and $\theta$--AB phases. Key readouts: centroid drift $\langle y(t)\rangle$, interferometric phase vs. $\oint A_\theta d\theta$, and Fourier spectra showing $\Delta E\approx \hbar^2/(2I)$.

\paragraph{Verification simulation: Bell correlation (sanity check).}
As an internal verification step (independent of the $X$--$\theta$ experimental program), I reproduce the standard singlet correlation curve $E(a,b)=-\cos(a-b)$ and contrast it with a simple local hidden-variable sign model.
\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.85\linewidth]{bell_correlation.png}
	\caption{Verification simulation: Bell correlation curve. Quantum prediction (cosine) versus a simple deterministic local hidden-variable sign model. The plotted curves are generated by the repository notebook and exported as a raster figure for inclusion in this paper.\label{fig:bell-correlation}}
\end{figure}
